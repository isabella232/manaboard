type TimeSummary @entity {
  id: ID!
  timestamp: BigInt
  burned: BigInt
}

type Latest @entity {
  id: ID!
  supply: BigInt
  burned: BigInt
  votes: BigInt
}

type VoteSummary @entity {
  id: ID!
  timestamp: BigInt
  accumulated: BigInt
  votes: BigInt
}

